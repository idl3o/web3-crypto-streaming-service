/**
 * Dashboard JavaScript for CryptoStream
 * Legacy compatibility file that redirects to the 2023 version
 */

// Import the 2023 dashboard script
document.addEventListener('DOMContentLoaded', function () {
    // Load the 2023 version of the dashboard script
    const script = document.createElement('script');
    script.src = 'dashboard-2023.js';
    document.body.appendChild(script);

    console.log('CryptoStream Dashboard - Legacy script loading 2023 version');
});

// This file exists for backwards compatibility
// The main dashboard functionality is in dashboard-2023.js
